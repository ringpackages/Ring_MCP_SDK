# The Main File

func main
	C_LINESIZE = 80
	? copy("=",C_LINESIZE)
	? "The Ring_MCP_SDK Package"
	? copy("=",C_LINESIZE)
	? "Ring_MCP_SDK package using the Ring programming language"
	? "See the folder : " + exefolder()+"/../libraries/Ring_MCP_SDK"
	? copy("=",C_LINESIZE)
	? "For examples see the folder : " + exefolder()+"/../samples/UsingMcpSdk"
	? copy("=",C_LINESIZE)